index.html

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Product Page</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background-color: #f4f4f4;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    select {
      margin: 10px;
      padding: 5px;
      font-size: 16px;
    }

    #products {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
      margin-top: 30px;
    }

    .product-card {
      background: #fff;
      border: 1px solid #ccc;
      border-radius: 8px;
      padding: 15px;
      width: 220px;
      text-align: center;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .product-card img {
      width: 100%;
      height: 240px;
      object-fit: contain;
      border-radius: 5px;
    }

    .product-name {
      font-size: 18px;
      margin: 10px 0;
      color: #444;
    }

    .product-price {
      font-weight: bold;
      color: #2a8f2a;
    }

    .product-category {
      font-size: 14px;
      color: #777;
    }
  </style>
</head>
<body>
  <h1>Products</h1>
  <select id="filterCategory">
    <option value="">All Categories</option>
    <option value="Clothing">Clothing</option>
    <option value="Electronics">Electronics</option>
  </select>

  <select id="sortPrice">
    <option value="">Sort by</option>
    <option value="low">Price: Low to High</option>
    <option value="high">Price: High to Low</option>
  </select>

  <div id="products"></div>

  <script>
    const products = [
      {
        name: "Stylish Jeans",
        price: 999,
        category: "Clothing",
        image: "28-kttladiesjeans1081-kotty-original-imagkybrucv6vrfc.webp"
      },
      {
        name: "Graphic T-Shirt",
        price: 499,
        category: "Clothing",
        image: "OIP.webp"
      },
      {
        name: "Smartphone",
        price: 15999,
        category: "Electronics",
        image: "photo.jpeg"
      },
      {
        name: "Laptop",
        price: 55999,
        category: "Electronics",
        image: "laptop.jpeg"
      }
    ];

    const productContainer = document.getElementById('products');
    const filterCategory = document.getElementById('filterCategory');
    const sortPrice = document.getElementById('sortPrice');

    function displayProducts(filteredProducts) {
      productContainer.innerHTML = '';
      filteredProducts.forEach(product => {
        productContainer.innerHTML += `
          <div class="product-card">
            <img src="${product.image}" alt="${product.name}">
            <div class="product-name">${product.name}</div>
            <div class="product-category">${product.category}</div>
            <div class="product-price">₹${product.price}</div>
          </div>
        `;
      });
    }

    function applyFilters() {
      let filtered = [...products];

      const selectedCategory = filterCategory.value;
      const sortValue = sortPrice.value;

      if (selectedCategory) {
        filtered = filtered.filter(p => p.category === selectedCategory);
      }

      if (sortValue === 'low') {
        filtered.sort((a, b) => a.price - b.price);
      } else if (sortValue === 'high') {
        filtered.sort((a, b) => b.price - a.price);
      }

      displayProducts(filtered);
    }

    filterCategory.addEventListener('change', applyFilters);
    sortPrice.addEventListener('change', applyFilters);

    // Initial display
    displayProducts(products);
  </script>
</body>
</html>


photos
C:\Users\srina\Desktop\fourthprojects\product-page\laptop.jpeg
C:\Users\srina\Desktop\fourthprojects\product-page\OIP.webp
C:\Users\srina\Desktop\fourthprojects\product-page\photo.jpeg
C:\Users\srina\Desktop\fourthprojects\product-page\28-kttladiesjeans1081-kotty-original-imagkybrucv6vrfc.webp
